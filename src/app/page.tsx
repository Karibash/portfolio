import * as styles from './page.css';

import type { FC } from 'react';

const HomePage: FC = () => {
  return (
    <main className={styles.bold}>
      HomePage
    </main>
  );
};

export default HomePage;
